import {navy, offwhite, white} from '@/style/colors'
import {style} from '@vanilla-extract/css'

export const animatedArticle = style({
  background: offwhite,
  height: '70vh',
  borderRadius: 30,
  marginBottom: 40,
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  color: navy
})
